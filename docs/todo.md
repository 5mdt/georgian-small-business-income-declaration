Bugs:

- when there are more than one transaction in one date, sorting works inconsistently

Features:
- make YTD income field **bold** only for the last transaction in month for each user.
- Total income should be separated by user. Supertotal income is useful too
- tint rows with user's color in table
- Add Yearly total separator in table
- auto export before schema migration
- local storage prefix
- add pop-up hints on buttons
- hide/grey out "load demo data button" when transactions exist
- suggest make backup before wiping all data
- Some kind of bundling page and all js in one request
- move user management block to sidebar
- inform if currency API is limited/unavailable
- option to save transaction even if currency exchange rate  data cannot be retrieved
- make numbers after decimal separator greyed out (css-class)
